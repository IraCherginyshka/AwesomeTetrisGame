export interface UserData {
  userInformation: {
    username: string;
    password: string;
  };
  playerInformation: {
    gender: string;
    dateOfBirth: string;
    country: string;
    avatar: string;
  };
}
